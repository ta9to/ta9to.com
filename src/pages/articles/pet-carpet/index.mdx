import { ArticleLayout } from '@/components/ArticleLayout'
import Image from 'next/future/image'
import nocoffee from './nocoffee.webp'

export const meta = {
  author: 'Takuto Kudo',
  date: '2022-11-05',
  title: 'ナチュラルウッド系の床に合うラグ',
  description:
    'ナチュラルウッド系の床に合うラグを見つけては追加していくページ。',
}

export const items = [
  {
    image: nocoffee,
    name: 'NOCOFFEE',
    url: 'https://www.low-ya.com/goods/LZS1C',
    size: '幅185×奥行185cm',
    price: '¥18,990'
  },
]

export function Table() {
  return (
    <table className="min-w-full divide-y divide-gray-300">
      <thead className="bg-gray-50">
        <tr>
          <th scope="col" className="py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-gray-900 sm:pl-6">
            品名
          </th>
          <th scope="col" className="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">
            値段
          </th>
          <th scope="col" className="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">
            サイズ
          </th>
        </tr>
      </thead>
      <tbody className="divide-y divide-gray-200 bg-white">
        {items.map((item) => (
          <tr key={item.email}>
            <td className="whitespace-nowrap py-4 pl-4 pr-3 text-sm sm:pl-6" style={{verticalAlign: 'middle'}}>
              <div className="flex items-center">
                <div className="h-10 w-10 flex-shrink-0">
                  <Image className="h-10 w-10" src={item.image} alt="" style={{borderRadius:'0px'}} />
                </div>
                <div className="ml-4">
                  <div className="font-medium text-gray-900"><a target='_blank' href={item.url}>{item.name}</a></div>
                </div>
              </div>
            </td>
            <td className="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
              {item.price}
            </td>
            <td className="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
              {item.size}
            </td>
          </tr>
        ))}
      </tbody>
    </table>
  )
}

export default (props) => <ArticleLayout meta={meta} {...props} />

ナチュラルウッド系の床に合うラグを見つけては追加していくページ。

<Table></Table>

